import 'package:deshi_mart/widgets/OurPage.dart';
import 'package:flutter/material.dart';

class WebDashBoard extends StatelessWidget {
  const WebDashBoard({super.key});

  @override
  Widget build(BuildContext context) {
    return OurPage(
        title: "Dashboard",
        child: Column(
          children: [],
        ));
  }
}
